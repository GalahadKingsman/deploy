# Vite бандлит vite.config в node_modules/.vite-temp; при linker=isolated импорт @vitejs/*
# иногда не резолвится. Hoisted дерево как у npm — стабильная сборка webapp на сервере/CI.
node-linker=hoisted
