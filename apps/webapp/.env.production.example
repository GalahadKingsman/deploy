# Скопируй в apps/webapp/.env.production или задай переменные перед vite build.
# Mini App на отдельном домене ОБЯЗАТЕЛЬНО указывает API, иначе запросы уйдут на тот же origin (nginx отдаст HTML).
VITE_API_BASE_URL=https://api.example.com
VITE_USE_MSW=0
VITE_REAL_API=1
